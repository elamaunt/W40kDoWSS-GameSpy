<UserControl x:Class="SteamSpy.Views.Pages.MainPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SteamSpy.Views.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <DockPanel Margin="20 0 0 20" Grid.Row="2" LastChildFill="False">
            <Button Width="250" Height="70" Command="{Binding LaunchGameCommand}"
                DockPanel.Dock="Left" Content="{DynamicResource LaunchGame}" Style="{DynamicResource LaunchButtonStyle}">
            </Button>
        </DockPanel>
    </Grid>
</UserControl>
